#NHM R course day 3
#23/01/25
#James Witts (Natalie Cooper)
#Reproducibility and Git (repo crispy-palm-tree)
#Script 1

#hello!!!
#This is the 3rd script I've done
#And I keep updating it.

# srisarya code :)
srishti_data<-data.frame(x=1:10,
                         y=1:10)

ggplot(srishti_data, aes(x = x, y = y))+
  geom_point(colour="darkcyan")+
  theme_bw

# jdwitts15a code :)
jw_data<-data.frame(x=1:10, y=1:10)

ggplot(mydata, aes(x = x, y = y))+
  geom_point(colour="deeppink")+
  theme_bw